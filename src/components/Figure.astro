---
import { Image } from 'astro:assets';
interface Props { src: ImageMetadata; alt: string; }
const { src, alt } = Astro.props;
---
<figure>
  <Image src={src} alt={alt} widths={[640, 720, 1024, 1600]} sizes="(min-width: 46rem) 44rem, calc(100vw - 20px)" format="webp" quality={80} />
</figure>
